// be sure that the file extension is `.d.ts` otherwise
// the name of this file doesn't matter and can be anywhere in the `src` directory
declare namespace Routes {
  // this should be a union of all routes that are valid and cannot be auto generated
  type UserDefinedPaths = "/user/";
}
